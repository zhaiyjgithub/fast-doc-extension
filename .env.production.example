# Production bundle (`yarn build`). Copy to `.env.production.local` (gitignored).

# Deepgram — key used in the built extension (often a restricted / prod key)
VITE_DEEPGRAM_API_KEY=

# Optional: overrides VITE_DEEPGRAM_API_KEY in production builds only
# VITE_DEEPGRAM_API_KEY_PROD=
