# Local development (`yarn dev`). Copy to `.env.development.local` (gitignored).

# Deepgram — either set the shared key here, or use a dev-only override:
VITE_DEEPGRAM_API_KEY=

# Optional: overrides VITE_DEEPGRAM_API_KEY while developing only
# VITE_DEEPGRAM_API_KEY_DEV=
